@extends('layouts.admin') @section('content')<h1 class="mb-4">Tableau de bord</h1><div class="row g-4 mb-4">@foreach([['Utilisateurs actifs',$activeUsers,'success'],['Utilisateurs bloqués',$blockedUsers,'danger'],['Épargné ce mois',Number::currency($monthlyTotal,'EUR','fr'),'primary']] as $stat)<div class="col-md-4"><div class="card"><div class="card-body"><small class="text-muted">{{ $stat[0] }}</small><div class="display-6 text-{{ $stat[2] }}">{{ $stat[1] }}</div></div></div></div>@endforeach</div>@foreach([['10 utilisateurs récents',$users],['10 gains récents',$gains],['10 épargnes récentes',$savings]] as [$heading,$rows])<div class="card mb-4"><div class="card-header"><h5>{{ $heading }}</h5></div><div class="table-responsive"><table class="table"><thead><tr><th>Référence</th><th>Libellé</th><th>Date</th></tr></thead><tbody>@foreach($rows as $row)<tr><td>{{ $row->uuid ?? $row->id }}</td><td>{{ $row->email ?? ($row->user?->email.' — '.Number::currency($row->amount,$row->currency,'fr')) }}</td><td>{{ $row->created_at?->format('d/m/Y H:i') }}</td></tr>@endforeach</tbody></table></div></div>@endforeach@endsection
