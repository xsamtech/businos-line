@extends('layouts.public') @section('content')<section class="py-5"><div class="container"><h1>{{ __('messages.gains') }}</h1><div class="alert alert-info">{{ __('messages.gain_rule') }}</div><div class="row g-3">@forelse($gains as $gain)<div class="col-md-4"><div class="card shadow-sm"><div class="card-body"><h5>{{ $gain->month }}/{{ $gain->year }}</h5><p class="h4">{{ Number::currency($gain->amount,$gain->currency,app()->getLocale()) }}</p><small>{{ __('messages.general_interest') }}: 10%</small></div></div></div>@empty<p>{{ __('messages.no_gains') }}</p>@endforelse</div></div></section>@endsection
