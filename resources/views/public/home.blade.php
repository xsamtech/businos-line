@extends('layouts.public') @section('content')
<header class="masthead"><div class="container"><div class="row align-items-center"><div class="col-lg-6 text-white"><span class="badge bg-warning text-dark mb-3">{{ __('messages.secure_tontine') }}</span><h1>{{ __('messages.hero_title') }}</h1><p class="lead">{{ __('messages.hero_text') }}</p><a class="btn btn-warning btn-lg" href="{{ route('register') }}">{{ __('messages.join') }}</a></div><div class="col-lg-6"><img class="img-fluid rounded-4 shadow" src="{{ asset('assets/img/tontine-workflow.png') }}" alt="{{ __('messages.workflow_alt') }}"></div></div></div></header>
<section class="py-5 bg-light"><div class="container"><div class="row g-4 text-center">@foreach([['bi-wallet2','feature_save','feature_save_text'],['bi-arrow-repeat','feature_turn','feature_turn_text'],['bi-shield-check','feature_secure','feature_secure_text']] as $feature)<div class="col-md-4"><div class="card h-100 border-0 shadow-sm p-4"><i class="bi {{ $feature[0] }} display-4 text-primary"></i><h3>{{ __('messages.'.$feature[1]) }}</h3><p>{{ __('messages.'.$feature[2]) }}</p></div></div>@endforeach</div></div></section>
<section class="py-5"><div class="container"><h2 class="text-center mb-4">{{ __('messages.monthly_history') }}</h2><div class="row g-3">@forelse($months as $month)<div class="col-md-4"><div class="card border-0 shadow-sm"><div class="card-body"><h5>{{ DateTime::createFromFormat('!m',(string)$month->month)->format('F') }} {{ $month->year }}</h5><div>{{ __('messages.contributors') }}: <strong>{{ $month->contributors }}</strong></div><div>{{ __('messages.collected') }}: <strong>{{ Number::currency($month->total,'EUR',app()->getLocale()) }}</strong></div></div></div></div>@empty<p class="text-center text-muted">{{ __('messages.no_history') }}</p>@endforelse</div></div></section>
@endsection
