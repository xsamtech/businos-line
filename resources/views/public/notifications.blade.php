@extends('layouts.public') @section('content')<section class="py-5"><div class="container"><h1>{{ __('messages.notifications') }}</h1><div class="list-group">@forelse($notifications as $notification)<div class="list-group-item d-flex justify-content-between"><span>{{ __('messages.notification_'.$notification->type) }}</span><small>{{ $notification->created_at->diffForHumans() }}</small></div>@empty<p>{{ __('messages.no_notifications') }}</p>@endforelse</div></div></section>@endsection
