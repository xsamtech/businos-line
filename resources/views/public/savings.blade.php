@extends('layouts.public') @section('title',__('messages.savings')) @section('content')<section class="py-5"><div class="container"><div class="d-flex justify-content-between align-items-center mb-4"><h1>{{ __('messages.savings') }}</h1><button class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#payModal">{{ __('messages.save_now') }}</button></div><div class="row g-3">@forelse($savings as $saving)<div class="col-md-4"><div class="card shadow-sm"><div class="card-body"><h5>{{ $saving->month }}/{{ $saving->year }}</h5><span class="badge {{ $saving->is_saving_sent?'bg-success':'bg-danger' }}">{{ $saving->is_saving_sent?__('messages.saved'):__('messages.not_saved') }}</span>@if($saving->is_saving_sent)<p class="mt-3 h4">{{ Number::currency($saving->amount,$saving->currency,app()->getLocale()) }}</p>@endif</div></div></div>@empty<p>{{ __('messages.no_savings') }}</p>@endforelse</div></div></section><div class="modal fade" id="payModal"><div class="modal-dialog"><form class="modal-content ajax-form" method="post" action="{{ route('paypal.create') }}" data-redirect-response="true">@csrf<div class="modal-header"><h5>{{ __('messages.save_now') }}</h5><button class="btn-close" data-bs-dismiss="modal"></button></div><div class="modal-body"><label class="form-label">{{ __('messages.amount') }} (EUR)</label><input class="form-control" type="number" name="amount" min="1" step="0.01" required></div><div class="modal-footer"><button class="btn btn-primary">PayPal</button></div></form></div></div>@endsection
